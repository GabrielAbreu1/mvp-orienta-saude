{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm run typecheck:libs && pnpm --filter @workspace/triagem-web run build && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @workspace/db run push && node artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
